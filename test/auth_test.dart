import 'package:flutter_test/flutter_test.dart';

// Placeholder: el proyecto requiere Firebase Auth/Firestore para autenticar.
// Este archivo se mantiene para que `flutter analyze` no falle por tests incompletos.
void main() {
  test('placeholder', () async {
    expect(true, isTrue);
  });
}

